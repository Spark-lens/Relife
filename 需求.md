# Relife 股票持仓与预警 MVP 需求

## 1. 背景与目标

Relife 第一版是个人投资辅助系统，用于统一查看 A 股和美股持仓，并在价格或日线技术指标触发时发送邮件提醒。

系统只做信息展示、指标计算和预警通知，不提供投资建议，不接入券商交易接口，不自动下单。实际挂单、成交、撤单、资金划转仍在银河证券、嘉信、盈透等官方交易工具中完成。

第一版目标是先形成稳定雏形：能导入/生成基础持仓，能在私有 Web 看板查看，能按规则触发提醒。功能不追求交易系统级实时性。

## 2. 第一版范围

### 2.1 账户与市场

第一版纳入以下账户和市场：

| 市场 | 账户/券商 | 第一版数据来源 | 资金/持仓基准 | 第一版能力 |
| --- | --- | --- | --- | --- |
| A 股 | 银河证券 | 内置模拟数据 | `159915.SZ`、`512100.SH`、`161716.SZ` 各 1000 股，成本价均为 1.00 CNY | 持仓展示、价格预警、日线指标预警 |
| 美股 | 嘉信 Schwab | 嘉信官网原始交易流水 CSV | `Individual_XXX389_Transactions_20260615-092158.csv` | 持仓展示、价格预警、日线指标预警 |
| 美股/现金 | 盈透 IBKR | 内置现金账户 | 50000 CNY 现金 | 现金展示，不导入证券持仓 |

### 2.2 MVP 必须实现

- 私有 Web 看板：手机和电脑都可访问。
- 嘉信原始交易流水 CSV 导入并归集持仓。
- 内置 A 股模拟持仓和 IBKR 现金账户。
- 按账户、市场、标的、币种维度汇总。
- 展示现价、成本、市值、浮动盈亏、仓位占比；成本未知时显示未知并排除盈亏计算。
- 价格预警：高于/低于指定价格时触发。
- 日线技术指标预警：250 日均线、布林线上轨、布林线下轨。
- 邮件通知：仅发送到 `2374423908@qq.com`。
- Cloudflare Workers + Cron + D1 部署方案。
- 免费/低成本行情源适配，按 best-effort 运行。

### 2.3 第一版明确不做

- 不接入券商交易 API。
- 不自动下单。
- 不做多用户系统和复杂权限系统，只做个人访问保护。
- 不做秒级或 30 秒级实时行情。
- 不承诺交易级实时性和行情准确性。
- 不做复杂品类建模；ETF、LOF、现金类证券先作为普通 symbol 展示。
- 不直接支持盈透、TradingView、银河证券等其他原始导出格式；统一持仓 CSV 模板也后置。
- 不做 SMTP 邮件发送、多行情源 fallback、精确交易日历、邮件服务恢复通知、TradingView 风格界面。

## 3. 运行平台

默认使用 Cloudflare：

- Cloudflare Workers：运行 Web API、页面服务、预警任务。
- Cloudflare Cron Triggers：每分钟触发预警检查。
- Cloudflare D1：保存持仓、现金、交易流水、规则、行情快照、通知记录、系统状态。
- Cloudflare Secrets：保存访问令牌、行情源密钥、邮件 API 密钥。

本地 Windows 仅用于开发调试，不作为正式运行平台。

## 4. 访问与安全

第一版只面向个人使用。

- Web 看板必须要求访问令牌。
- 访问令牌通过环境变量 `APP_ACCESS_TOKEN` 配置。
- API 请求可通过 `Authorization: Bearer <token>` 或查询参数 `?token=<token>` 访问。
- 默认不开放注册、登录、找回密码、多用户权限。
- D1 中不保存券商登录密码、交易密码、银行卡信息。
- 邮件和行情 API 密钥只保存在 Cloudflare Secrets，不写入仓库。

## 5. 持仓与现金数据

### 5.1 嘉信交易流水导入

第一版支持导入嘉信官网导出的交易记录 CSV，接口为：

```text
POST /api/import/schwab-transactions
```

导入文件表头基于当前样例：

```csv
Date,Action,Symbol,Description,Quantity,Price,Fees & Comm,Amount
```

当前验收样例文件为：

```text
Individual_XXX389_Transactions_20260615-092158.csv
```

### 5.2 嘉信归集规则

- `Buy`：增加数量和持仓成本。
- `Sell`：减少数量，并按移动平均成本扣减持仓成本。
- `Security Transfer`：增加数量；如果流水没有价格，优先查内置成本覆盖配置。
- 正向 `Journal`：增加数量；如果流水没有价格，优先查内置成本覆盖配置。
- 股息、税费等现金事件不形成证券持仓，但记录为现金流事件。
- 归集后数量为 0 的标的不进入当前持仓列表。
- 同一 `account + market + symbol` 视为同一持仓。
- 嘉信默认账户标识为 `schwab`，市场为 `US`，币种为 `USD`。

嘉信成本覆盖配置第一版内置：

| account | symbol | avg_cost | currency | 说明 |
| --- | --- | ---: | --- | --- |
| `schwab` | `BOXX` | 115.5 | USD | 转仓进入，原始流水无成本价 |

当前样例导入后的预期持仓：

| symbol | quantity | avg_cost | currency |
| --- | ---: | ---: | --- |
| `BOXX` | 22 | 115.5 | USD |
| `QQQI` | 3 | 49.7833 | USD |
| `SOXS` | 1 | 14.35 | USD |

### 5.3 A 股模拟数据

第一版内置以下 A 股模拟持仓，不依赖券商导入：

| account | broker | market | symbol | name | quantity | avg_cost | currency |
| --- | --- | --- | --- | --- | ---: | ---: | --- |
| `yinhe` | 银河证券 | CN | `159915.SZ` | 创业板 ETF | 1000 | 1.00 | CNY |
| `yinhe` | 银河证券 | CN | `512100.SH` | 中证 1000 ETF | 1000 | 1.00 | CNY |
| `yinhe` | 银河证券 | CN | `161716.SZ` | 招商双债 ETF | 1000 | 1.00 | CNY |

### 5.4 IBKR 现金账户

第一版 IBKR 只展示现金，不导入证券持仓：

| account | broker | currency | cash |
| --- | --- | --- | ---: |
| `ibkr` | IBKR | CNY | 50000 |

## 6. Web 看板

首页直接展示投资组合，不做营销页。

必须包含：

- 总资产概览：按币种分别展示，不强制做汇率换算。
- 市场分组：A 股、美股、现金。
- 账户分组：银河证券、嘉信、盈透。
- 持仓表格：标的、名称、数量、成本、现价、市值、盈亏、盈亏比例、优先级、预警状态。
- 现金表格：账户、币种、现金余额。
- 系统状态：最近行情更新时间、最近预警检查时间、最近邮件发送状态。

默认排序：

1. 有启用预警的标的优先。
2. `priority` 数字小的优先。
3. 市场顺序：美股、A 股、现金。
4. 账户顺序：嘉信、盈透、银河证券。

必须支持按市场、账户、是否启用预警筛选。手机浏览器必须可读，窄屏下持仓表格允许横向滚动。

## 7. 行情与指标

### 7.1 行情源策略

第一版使用可替换的行情 provider 接口。

- 美股和 A 股都使用免费/非官方/个人用途行情源作为 best-effort 数据。
- 行情源实现必须封装在独立模块，后续可替换为付费官方 API。
- 行情源失败时不得导致系统崩溃。
- 行情数据只用于个人提醒，不作为交易执行依据。

### 7.2 检查频率

价格预警：

- Cloudflare Cron 每 1 分钟触发一次。
- 每次只检查启用预警的标的。
- 当行情源限流或失败时，按 `priority` 从小到大检查。
- 目标监控规模：A 股 + 美股合计不超过 40 只，其中启用 1 分钟价格预警的标的不超过 20 只。

日线指标：

- 每个交易日收盘后计算。
- 第一版可以用自然日近似交易日历，但指标计算必须跳过无行情数据日期。

### 7.3 指标定义

250 日均线：

- 使用最近 250 个有效交易日的收盘价。
- 当历史数据不足 250 个有效交易日时，不触发该指标预警，并在看板显示 `数据不足`。

布林线：

- 周期：20 个有效交易日。
- 标准差倍数：2。
- 中轨：20 日收盘价简单移动平均。
- 上轨：中轨 + 2 * 20 日收盘价标准差。
- 下轨：中轨 - 2 * 20 日收盘价标准差。
- 当历史数据不足 20 个有效交易日时，不触发布林线预警。

## 8. 预警规则

第一版支持以下规则类型：

| 类型 | 说明 |
| --- | --- |
| `price_above` | 当前价格大于等于指定价格时触发 |
| `price_below` | 当前价格小于等于指定价格时触发 |
| `ma250_cross_up` | 收盘价从下向上突破 250 日均线 |
| `ma250_cross_down` | 收盘价从上向下跌破 250 日均线 |
| `bollinger_upper_break` | 收盘价突破布林线上轨 |
| `bollinger_lower_break` | 收盘价跌破布林线下轨 |

每条预警规则包含：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 规则 ID |
| `market` | string | `US` 或 `CN` |
| `account` | string | 账户，可为空；为空表示对该市场该标的生效 |
| `symbol` | string | 标的代码 |
| `type` | string | 规则类型 |
| `threshold` | number | 价格阈值，仅价格规则需要 |
| `enabled` | boolean | 是否启用 |
| `cooldown_minutes` | integer | 同一规则冷却时间，默认 1440 分钟 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

触发规则：

- 同一规则在冷却时间内只发送一次邮件。
- 价格规则按盘中最新价格判断。
- 技术指标规则按日线收盘价判断。
- 技术指标必须判断“突破/跌破”，不能只因为价格持续在均线或布林线外就每天重复触发。
- 禁用规则后不再触发，但保留历史通知记录。

## 9. 邮件通知

第一版仅支持邮件通知。

- 收件人固定为 `2374423908@qq.com`。
- 不支持 Telegram、微信、企业微信、短信、App 推送。
- 邮件 provider 第一版只实现 `resend` 和 `mock`。

必须通过环境变量配置：

| 环境变量 | 说明 |
| --- | --- |
| `MAIL_PROVIDER` | 默认 `mock`，部署时可设为 `resend` |
| `ALERT_EMAIL_TO` | 默认 `2374423908@qq.com` |
| `RESEND_API_KEY` | Resend API Key |
| `MAIL_FROM` | 发件人地址 |

邮件标题格式：

```text
[Relife] {market} {symbol} {rule_type} triggered
```

邮件正文必须包含市场、账户、标的代码和名称、当前价格或收盘价、触发规则、阈值或指标值、触发时间、数据源标识，以及“不构成投资建议”的提醒说明。

邮件发送失败时记录错误和通知记录，不阻塞 Cron 主流程。

## 10. 数据库需求

第一版至少需要以下数据表或等价结构：

- `holdings`：持仓，包含 `account`、`market`、`symbol`、`name`、`quantity`、`avg_cost`、`currency`、`cost_source`。
- `cash_balances`：现金账户余额。
- `transactions`：嘉信原始流水的规范化结果，便于重算持仓。
- `alert_rules`：预警规则。
- `quotes`：最新行情快照。
- `daily_bars`：日线历史数据。
- `indicator_snapshots`：指标快照。
- `notifications`：通知发送记录。
- `system_events`：行情源、Cron、邮件等系统事件。

关键唯一约束：

- `holdings`: `account + market + symbol` 唯一。
- `cash_balances`: `account + currency` 唯一。
- `quotes`: `market + symbol` 唯一保存最新快照。
- `alert_rules`: `id` 唯一。

## 11. API 需求

第一版技术栈采用 TypeScript Cloudflare Worker，路由可使用 Hono，数据存储使用 D1。

必须提供：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/` | 私有 Web 看板 |
| `POST` | `/api/import/schwab-transactions` | 导入嘉信原始交易流水 CSV |
| `GET` | `/api/holdings` | 获取持仓、现金和汇总 |
| `GET` | `/api/alerts` | 获取预警规则 |
| `POST` | `/api/alerts` | 创建预警规则 |
| `PATCH` | `/api/alerts/:id` | 更新或启停预警规则 |
| `DELETE` | `/api/alerts/:id` | 删除预警规则 |
| `POST` | `/api/jobs/check-alerts` | 手动触发预警检查，仅调试使用 |
| `POST` | `/api/jobs/update-daily-bars` | 手动触发日线更新，仅调试使用 |
| `GET` | `/api/system/status` | 获取系统状态 |

所有 API 都必须校验访问令牌。

## 12. 验收标准

### 12.1 嘉信导入

- 能导入 `Individual_XXX389_Transactions_20260615-092158.csv`。
- 能正确分类 `Buy`、`Sell`、`Security Transfer`、`Journal`、股息和税费动作。
- 能按移动平均成本归集持仓。
- 能使用 `BOXX = 115.5 USD` 成本覆盖配置。
- 导入后生成 `BOXX 22 @ 115.5`、`QQQI 3 @ 49.7833`、`SOXS 1 @ 14.35`。
- 不产生数量为 0 的当前持仓。

### 12.2 种子数据

- A 股三只模拟标的数量均为 1000 股，成本均为 1.00 CNY。
- IBKR 只显示 50000 CNY 现金账户。

### 12.3 看板

- 能按市场、账户展示持仓和现金。
- 能展示 A 股 CNY、美股 USD、IBKR CNY 现金，不强制换算总资产。
- 能展示现价、市值、浮盈亏、浮盈亏比例。
- 成本未知的持仓显示未知，并排除盈亏计算。
- 能展示最近行情更新时间、最近预警检查时间、最近邮件状态。
- 手机浏览器可读，不出现明显布局重叠。

### 12.4 价格预警

- `price_above` 达到阈值后发送邮件。
- `price_below` 达到阈值后发送邮件。
- 冷却时间内不重复发送同一规则邮件。
- 行情源失败时记录系统事件，不导致 Cron 失败退出。

### 12.5 技术指标预警

- 固定历史数据下，250 日均线计算结果正确。
- 固定历史数据下，20 日布林线上轨、下轨计算结果正确。
- 历史数据不足时不触发，并显示 `数据不足`。
- 突破类规则只在穿越发生时触发，不因持续站上/跌破而每日重复。

### 12.6 邮件通知

- `mock` provider 不真实发送邮件。
- `resend` provider 请求体包含规则、价格/指标值、时间、数据源和免责声明。
- 邮件只发送到 `2374423908@qq.com`。
- 邮件发送失败会记录错误和通知记录。

### 12.7 平台

- 本地可以模拟 D1 和 Cron 流程。
- 部署到 Cloudflare 后，Cron 每分钟触发检查任务。
- D1 migration 可以从空库初始化。
- Secrets 不进入 Git 仓库。

## 13. 实施优先级

### P0

- 项目脚手架。
- D1 schema 和 migration。
- 访问令牌保护。
- 嘉信交易流水导入。
- A 股和 IBKR 种子数据。
- 持仓、现金和账户/市场汇总。

### P1

- 私有 Web 看板。
- 行情 provider 抽象。
- A 股和美股 latest quote 获取。
- 价格预警检查。
- 邮件 provider 抽象、mock provider、Resend provider。
- 通知记录和系统状态。
- Cloudflare Cron 配置。

### P2

- 日线历史数据更新。
- 250 日均线。
- 布林线。
- 技术指标突破预警。
- 移动端看板优化。

### P3

- 统一持仓 CSV 模板导入。
- 盈透、TradingView、银河证券原始导出格式。
- 多行情源 fallback。
- A 股交易日历精确化。
- 更完整的现金流和成本调整审计。

## 14. 默认假设

- 第一版是个人工具，不公开给他人使用。
- A 股和美股都进入第一版。
- A 股模拟数据按每只 1000 股入库，不按 1000 手换算。
- `159915` 归为 `159915.SZ`，`512100` 归为 `512100.SH`，`161716` 归为 `161716.SZ`。
- 嘉信当前以 2026-06-15 导出的 CSV 为第一版样例和验收基准。
- IBKR 使用 50000 CNY 作为现金展示基准，除非后续提供流水文件。
- 通知方式只用邮件，目标邮箱固定为 `2374423908@qq.com`。
- A 股和美股合计持仓展示不超过 40 只。
- 启用 1 分钟价格预警的标的不超过 20 只。
- 免费行情源可能延迟、失败或限流，系统按 best-effort 处理。
- 资金和盈亏默认按原币种展示，不在第一版强制做 CNY/USD 汇率换算。
