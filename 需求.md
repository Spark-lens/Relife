# Relife 股票持仓与预警 MVP 需求

## 1. 背景与目标

Relife 第一版是个人投资辅助系统，用于统一查看 A 股和美股持仓，并在价格或技术指标触发时发送邮件提醒。

系统只做信息展示、指标计算和预警通知，不提供投资建议，不接入券商交易接口，不自动下单。实际挂单、成交、撤单、资金划转仍在银河证券、嘉信、盈透等官方交易工具中完成。

## 2. 第一版范围

### 2.1 账户与市场

第一版纳入以下账户和市场：

| 市场 | 账户/券商 | 账户定位 | 资金信息 | 第一版能力 |
| --- | --- | --- | --- | --- |
| A 股 | 银河证券 | 小仓位试水，观察市场回撤 | 10000 CNY | 持仓展示、价格预警、日线指标预警 |
| 美股 | 嘉信 Schwab | 频繁交易 | 3060 USD | 持仓展示、价格预警、日线指标预警 |
| 美股 | 盈透 IBKR | 低频交易 | 50000 CNY | 持仓展示、价格预警、日线指标预警 |

### 2.2 MVP 必须实现

- 私有 Web 看板：手机和电脑都可访问。
- 统一 CSV 导入持仓。
- A 股和美股持仓展示。
- 账户、市场、标的、币种维度汇总。
- 当前价格、成本、市值、浮动盈亏、仓位占比展示。
- 价格预警：高于/低于指定价格时触发。
- 日线技术指标预警：250 日均线、布林线上轨、布林线下轨。
- 邮件通知：仅发送到 `2374423908@qq.com`。
- Cloudflare Workers + Cron + D1 部署方案。
- 免费/低成本行情源适配，按 best-effort 运行。

### 2.3 第一版明确不做

- 不接入券商交易 API。
- 不自动下单。
- 不做期权、基金、债券、加密货币。
- 不做秒级或 30 秒级实时行情。
- 不承诺交易级实时性和行情准确性。
- 不做多用户系统。
- 不做复杂权限系统，只做个人访问保护。

## 3. 运行平台

### 3.1 默认平台

默认使用 Cloudflare：

- Cloudflare Workers：运行 Web API、页面服务、预警任务。
- Cloudflare Cron Triggers：每分钟触发预警检查。
- Cloudflare D1：保存持仓、规则、行情快照、通知记录、系统状态。
- Cloudflare Secrets：保存访问令牌、行情源密钥、邮件 API 密钥。

选择原因：

- 无需自购服务器。
- 可长期在线。
- 适合小规模个人项目。
- Workers Cron 支持分钟级定时，符合 1 分钟 best-effort 预警目标。

### 3.2 备选平台

- GitHub Actions：可作为低频备用方案，但定时粒度和稳定性不适合作为 1 分钟预警主方案。
- 本地 Windows：仅用于开发调试，不作为正式运行平台。
- 手机常驻：不作为第一版方案，移动系统后台限制会影响可靠性。
- Render 免费服务：可能空闲休眠，不作为预警主方案。

## 4. 访问与安全

第一版只面向个人使用。

- Web 看板必须要求访问令牌。
- 访问令牌通过环境变量 `APP_ACCESS_TOKEN` 配置。
- API 请求可通过 `Authorization: Bearer <token>` 或查询参数 `?token=<token>` 访问。
- 默认不开放注册、登录、找回密码、多用户权限。
- D1 中不保存券商登录密码、交易密码、银行卡信息。
- 邮件和行情 API 密钥只保存在 Cloudflare Secrets，不写入仓库。

## 5. 持仓导入

### 5.1 导入方式

第一版只支持统一 CSV 模板。用户可以从嘉信、盈透、TradingView 或银河证券导出数据后，手动整理为统一模板再导入。

暂不直接兼容各券商原始文件格式。

### 5.2 CSV 字段

CSV 使用 UTF-8 编码，第一行是表头。

必填字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `market` | string | 市场，取值 `US` 或 `CN` |
| `account` | string | 账户名称，例如 `schwab`、`ibkr`、`yinhe` |
| `broker` | string | 券商名称，例如 `Schwab`、`IBKR`、`银河证券` |
| `symbol` | string | 标的代码 |
| `quantity` | number | 持仓数量，可以为小数 |
| `avg_cost` | number | 持仓平均成本 |
| `currency` | string | 币种，A 股默认 `CNY`，美股默认 `USD` |
| `priority` | integer | 预警检查优先级，数字越小越优先 |

可选字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 标的名称 |
| `notes` | string | 备注 |

### 5.3 标的代码规范

美股：

- 使用大写 ticker，例如 `AAPL`、`MSFT`、`TSLA`。
- ETF 同样使用 ticker，例如 `VOO`、`QQQ`。

A 股：

- 使用带交易所后缀的规范代码。
- 上交所：`600519.SH`、`510300.SH`。
- 深交所：`000001.SZ`、`300750.SZ`。
- 第一版导入时如果没有后缀，应判定为无效数据并提示用户修正。

### 5.4 CSV 示例

```csv
market,account,broker,symbol,name,quantity,avg_cost,currency,priority,notes
US,schwab,Schwab,AAPL,Apple,10,180.50,USD,1,frequent trade account
US,ibkr,IBKR,VOO,Vanguard S&P 500 ETF,3,480.00,USD,2,long term
CN,yinhe,银河证券,600519.SH,贵州茅台,1,1500.00,CNY,1,A share trial
CN,yinhe,银河证券,300750.SZ,宁德时代,10,200.00,CNY,2,A share watch
```

### 5.5 导入规则

- 同一 `account + market + symbol` 视为同一持仓。
- 重复导入时默认覆盖该持仓的数量、成本、名称、备注、优先级。
- `quantity <= 0` 不允许导入。
- `avg_cost < 0` 不允许导入。
- `priority` 缺失时默认为 `100`。
- `currency` 缺失时根据 `market` 自动填充：`US -> USD`，`CN -> CNY`。
- 导入结果需要返回新增数量、更新数量、失败行数和失败原因。

## 6. Web 看板

### 6.1 首页信息结构

首页直接展示投资组合，不做营销页。

必须包含：

- 总资产概览：按币种分别展示，不强制做汇率换算。
- 市场分组：A 股、美股。
- 账户分组：银河证券、嘉信、盈透。
- 持仓表格：标的、名称、数量、成本、现价、市值、盈亏、盈亏比例、优先级、预警状态。
- 系统状态：最近行情更新时间、最近预警检查时间、最近邮件发送状态。

### 6.2 排序与筛选

默认排序：

1. 有启用预警的标的优先。
2. `priority` 数字小的优先。
3. 市场顺序：美股、A 股。
4. 账户顺序：嘉信、盈透、银河证券。

必须支持：

- 按市场筛选。
- 按账户筛选。
- 按是否启用预警筛选。

### 6.3 移动端要求

- 手机浏览器可正常查看。
- 持仓表格在窄屏下允许横向滚动。
- 关键数字不能重叠或溢出。
- 预警状态和系统状态在手机上必须可读。

## 7. 行情与指标

### 7.1 行情源策略

第一版使用可替换的行情 provider 接口。

默认策略：

- 美股和 A 股都使用免费/非官方/个人用途行情源作为 best-effort 数据。
- 系统必须把行情源实现封装在独立模块，后续可以替换为付费官方 API。
- 行情源失败时不得导致系统崩溃。
- 行情数据只用于个人提醒，不作为交易执行依据。

### 7.2 检查频率

价格预警：

- Cloudflare Cron 每 1 分钟触发一次。
- 每次只检查启用预警的标的。
- 当行情源限流或失败时，按 `priority` 从小到大检查。
- 目标监控规模：A 股 + 美股合计不超过 40 只，其中启用 1 分钟价格预警的标的不超过 20 只。

日线指标：

- 每个交易日收盘后计算。
- 美股按美东交易日处理。
- A 股按中国交易日处理。
- 第一版可以用自然日近似交易日历，但指标计算必须跳过无行情数据日期。

### 7.3 指标定义

250 日均线：

- 使用最近 250 个有效交易日的收盘价。
- 当历史数据不足 250 个有效交易日时，不触发该指标预警，并在看板显示 `数据不足`。

布林线：

- 周期：20 个有效交易日。
- 标准差倍数：2。
- 中轨：20 日收盘价简单移动平均。
- 上轨：中轨 + 2 * 20 日收盘价标准差。
- 下轨：中轨 - 2 * 20 日收盘价标准差。
- 当历史数据不足 20 个有效交易日时，不触发布林线预警。

## 8. 预警规则

### 8.1 支持的规则类型

第一版支持：

| 类型 | 说明 |
| --- | --- |
| `price_above` | 当前价格大于等于指定价格时触发 |
| `price_below` | 当前价格小于等于指定价格时触发 |
| `ma250_cross_up` | 收盘价从下向上突破 250 日均线 |
| `ma250_cross_down` | 收盘价从上向下跌破 250 日均线 |
| `bollinger_upper_break` | 收盘价突破布林线上轨 |
| `bollinger_lower_break` | 收盘价跌破布林线下轨 |

### 8.2 规则字段

每条预警规则包含：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | string | 规则 ID |
| `market` | string | `US` 或 `CN` |
| `account` | string | 账户，可为空；为空表示对该市场该标的生效 |
| `symbol` | string | 标的代码 |
| `type` | string | 规则类型 |
| `threshold` | number | 价格阈值，仅价格规则需要 |
| `enabled` | boolean | 是否启用 |
| `cooldown_minutes` | integer | 同一规则冷却时间，默认 1440 分钟 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

### 8.3 触发与去重

- 同一规则在冷却时间内只发送一次邮件。
- 价格规则按盘中最新价格判断。
- 技术指标规则按日线收盘价判断。
- 技术指标必须判断“突破/跌破”，不能只因为价格持续在均线或布林线外就每天重复触发。
- 禁用规则后不再触发，但保留历史通知记录。

## 9. 邮件通知

### 9.1 通知方式

第一版仅支持邮件通知。

- 收件人固定为 `2374423908@qq.com`。
- 不支持 Telegram。
- 不支持微信、企业微信、短信、App 推送。

### 9.2 邮件发送实现

Cloudflare Workers 默认使用 HTTP 邮件 API。第一版推荐使用 Resend HTTP API，因为其免费层支持个人小规模邮件发送，并提供 REST API。

邮件发送模块必须做成可替换 provider：

- `resend`：默认 provider，适合 Cloudflare Workers。
- `smtp`：仅作为本地开发或未来备用，不作为 Cloudflare Workers 默认方案。
- `mock`：测试环境使用，不真实发信。

必须通过环境变量配置：

| 环境变量 | 说明 |
| --- | --- |
| `MAIL_PROVIDER` | 默认 `resend` |
| `ALERT_EMAIL_TO` | 默认 `2374423908@qq.com` |
| `RESEND_API_KEY` | Resend API Key |
| `MAIL_FROM` | 发件人地址 |

### 9.3 邮件内容

邮件标题格式：

```text
[Relife] {market} {symbol} {rule_type} triggered
```

邮件正文必须包含：

- 市场。
- 账户。
- 标的代码和名称。
- 当前价格或收盘价。
- 触发规则。
- 阈值或指标值。
- 触发时间。
- 数据源标识。
- 提醒说明：该邮件仅为个人信息提醒，不构成投资建议。

### 9.4 失败处理

- 邮件发送失败时记录错误，不重复刷屏。
- 同一类系统错误连续发生时，只记录最近一次错误和累计次数。
- 邮件服务恢复后发送一次恢复通知。
- 通知记录必须保存到 D1，方便看板展示最近通知。

## 10. 数据库需求

第一版至少需要以下数据表或等价结构：

- `holdings`：持仓。
- `alert_rules`：预警规则。
- `quotes`：最新行情快照。
- `daily_bars`：日线历史数据。
- `indicator_snapshots`：指标快照。
- `notifications`：通知发送记录。
- `system_events`：行情源、Cron、邮件等系统事件。

关键唯一约束：

- `holdings`: `account + market + symbol` 唯一。
- `quotes`: `market + symbol` 唯一保存最新快照。
- `alert_rules`: `id` 唯一。

## 11. API 需求

第一版 Web/API 可采用 Workers 原生接口或轻量框架实现。

必须提供：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/` | 私有 Web 看板 |
| `POST` | `/api/import/holdings` | 导入持仓 CSV |
| `GET` | `/api/holdings` | 获取持仓列表和汇总 |
| `GET` | `/api/alerts` | 获取预警规则 |
| `POST` | `/api/alerts` | 创建预警规则 |
| `PATCH` | `/api/alerts/:id` | 更新或启停预警规则 |
| `DELETE` | `/api/alerts/:id` | 删除预警规则 |
| `POST` | `/api/jobs/check-alerts` | 手动触发预警检查，仅调试使用 |
| `POST` | `/api/jobs/update-daily-bars` | 手动触发日线更新，仅调试使用 |
| `GET` | `/api/system/status` | 获取系统状态 |

所有 API 都必须校验访问令牌。

## 12. 验收标准

### 12.1 CSV 导入

- 能导入包含 A 股和美股的统一 CSV。
- 能正确拒绝缺少 `market`、`account`、`symbol`、`quantity`、`avg_cost` 的行。
- 能拒绝 A 股无交易所后缀的代码。
- 重复导入同一 `account + market + symbol` 时更新已有持仓。
- 导入结果能显示成功、失败和失败原因。

### 12.2 看板

- 能按市场、账户展示持仓。
- 能展示 A 股 CNY 和美股 USD，不强制换算总资产。
- 能展示现价、市值、浮盈亏、浮盈亏比例。
- 能展示最近行情更新时间、最近预警检查时间、最近邮件状态。
- 手机浏览器可读，不出现明显布局重叠。

### 12.3 价格预警

- `price_above` 达到阈值后发送邮件。
- `price_below` 达到阈值后发送邮件。
- 冷却时间内不重复发送同一规则邮件。
- 行情源失败时记录系统事件，不导致 Cron 失败退出。

### 12.4 技术指标预警

- 固定历史数据下，250 日均线计算结果正确。
- 固定历史数据下，20 日布林线上轨、下轨计算结果正确。
- 历史数据不足时不触发，并显示 `数据不足`。
- 突破类规则只在穿越发生时触发，不因持续站上/跌破而每日重复。

### 12.5 邮件通知

- 只发送到 `2374423908@qq.com`。
- 邮件正文包含触发规则、价格/指标值、时间和数据源。
- 邮件发送失败会记录错误。
- 邮件服务恢复后发送一次恢复通知。

### 12.6 平台

- 本地可以模拟 D1 和 Cron 流程。
- 部署到 Cloudflare 后，Cron 每分钟触发检查任务。
- D1 migration 可以从空库初始化。
- Secrets 不进入 Git 仓库。

## 13. 实施优先级

### P0

- 项目脚手架。
- D1 schema 和 migration。
- 访问令牌保护。
- 统一 CSV 导入。
- 持仓列表和账户/市场汇总。
- 邮件 provider 抽象和 mock provider。

### P1

- 行情 provider 抽象。
- A 股和美股 latest quote 获取。
- 价格预警检查。
- Resend 邮件发送。
- 通知记录和系统状态。
- Cloudflare Cron 配置。

### P2

- 日线历史数据更新。
- 250 日均线。
- 布林线。
- 技术指标突破预警。
- 移动端看板优化。

### P3

- 更好看的 TradingView 风格持仓界面。
- 多行情源 fallback。
- 导入嘉信、盈透、TradingView 原始导出格式。
- A 股交易日历精确化。
- 后续券商交易消息接入调研。

## 14. 默认假设

- 第一版是个人工具，不公开给他人使用。
- A 股和美股都进入第一版。
- 通知方式只用邮件，目标邮箱固定为 `2374423908@qq.com`。
- A 股和美股合计持仓展示不超过 40 只。
- 启用 1 分钟价格预警的标的不超过 20 只。
- 免费行情源可能延迟、失败或限流，系统按 best-effort 处理。
- 资金和盈亏默认按原币种展示，不在第一版强制做 CNY/USD 汇率换算。
- 第一版优先保证稳定、可维护和低成本，而不是专业交易系统级实时性。
